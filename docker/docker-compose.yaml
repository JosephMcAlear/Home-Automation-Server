version: '3.9'

services:
  deployer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: repo_deployer
    ports:
      - "8080:8080"   # map host:container port (adjust as needed)
    environment:
      - SPRING_PROFILES_ACTIVE=prod   # optional environment variable
